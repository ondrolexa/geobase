[Rocks]
rocks.SiteID = "$SiteID"

[Photos]
photos.SiteID = "$SiteID"

[Samples]
__sql__ = "SELECT v.* FROM sites s JOIN rocks r ON s.SiteID = r.SiteID JOIN samples v ON r.RockID = v.RockID WHERE s.SiteID='$SiteID'"
action:visible=0

